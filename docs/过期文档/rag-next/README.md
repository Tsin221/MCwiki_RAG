# MCwiki RAG 下一阶段任务包

更新时间：2026-09-23

本目录保留 2026-09-23 已完成的三项基础建设任务。原任务 04～06 已取消，不再作为后续
执行计划。下一轮任务应以完善 RAG 系统能力为目标重新编写，不继续围绕固定评测结果做细碎优化。

## 当前基线

- 分支：`main`；起始提交至少包含 `20d18d9`；
- 固定回归：15 个可回答问题 + 3 个无答案问题；
- 期望来源进入证据：15/15；
- 严格引用题集指定来源：14/15；
- 引用编号有效：158/158；
- 无答案可靠拒答：3/3；
- 正确性 2.00/2、完整性 1.93/2、忠实度 2.00/2；
- 当前不引入 Reranker、GraphRAG、LangGraph 或开放互联网检索。

最新证据：`data/evaluation/answer_quality/REPORT-2026-09-22-reliability.md`。

## 已保留任务

| 任务 | 产出 | 状态 |
|---|---|---|
| 01 | 评测集 v2、结构校验和首批 48 题 | 已合入 `main` |
| 02 | BM25 四种查询策略的可复现诊断 | 已合入 `main`，不改变生产默认策略 |
| 03 | 相邻证据块合并和真实 18 题回归 | 已合入 `main` |

对应任务文件：

1. `01-evaluation-v2.md`；
2. `02-bm25-diagnostic.md`；
3. `03-evidence-assembly.md`。

原任务 04 的多要点编排实现未达到质量门槛，已从 Git 分支和 `main` 中排除。原任务 05、06
也随旧计划取消。下一轮重新布置时，不把它们作为默认后续顺序。

## 所有执行者共同遵守的规则

1. 不修改或覆盖 2026 年 7 月、9 月已有基线文件。
2. 不删除失败问题，不通过更改问题或标签掩盖退化。
3. 新实验使用新文件名，并记录数据集、配置、提示词和代码版本。
4. 每次只验证本任务提出的一个主要变量；不要顺手加入 Reranker 或 Agent 框架。
5. 不提交 `.env`、API Key、临时日志、IDE 配置或本地数据库副本。
6. 自动化测试通过后再运行真实模型评测；真实调用结果必须保存完整运行配置。
7. 若结果不优于基线，应保留实验报告并撤回默认行为，不得只提交“看起来更先进”的代码。
8. 每个执行者使用独立分支，建议名称见对应任务文档，并只提交自己的任务范围。

## 统一交接格式

每个执行者完成后应提供：

- 分支名和提交哈希；
- 修改文件清单；
- 执行过的验证命令及结果；
- 与基线逐项对比的指标；
- 失败案例和未解决风险；
- 是否建议合并，以及建议的下一任务输入。
